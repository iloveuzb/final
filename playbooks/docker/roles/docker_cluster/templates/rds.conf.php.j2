<?php
// Database configuration
$servername = "db101";
$username = "root";
$password = "{{ password }}"; // Can be passed from the playbook
$dbname = "countries";

// Create connection
$conn = new mysqli($servername, $username, $password, $dbname);
?>
